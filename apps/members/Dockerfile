# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build members`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t members`.
FROM docker.io/node:lts-alpine

WORKDIR /app

RUN addgroup --system members && \
          adduser --system -G members members

COPY dist/apps/members members
RUN #chown -R members:members .

RUN yarn install --production --frozen-lockfile --cwd members

CMD [ "node", "members/main.js" ]
